** 6/09/15 **

1. Mirar posibles inicializaciones de población a parte de la aleatoria.

2. Supongo que será suficiente, pero comprobar que levelSeedRandom es lo suficientemente bueno.

3. Codificar las restricciones de nivel: Compleción del mismo y que los elementos no se salgan del tamaño del nivel.

4. Dar valores a los parámetros h, w, wg, wbefore y wafter: Puede que sea necesario obtenerlos explícitamente.

5. Ver si el tipo de elemento 0 (plataforma inicial) es equivalente a una plataforma normal (tipo 1).

6. Al inicializar la población, comprobar que o bien se introduzcan como máximo maxElementsPerLevel o bien que se pase del ancho del nivel (3).

7. El proceso de generación de enemigos -> Se hace después de generar los elementos del nivel. Van encima de las plataformas por lo que no es necesario especificar la y. Mirar addEnemyLine.

8. Codificar el método de evaluación (evaluate).

9. Estudiar distintos métodos de cruce y selección.

** 7/09/15 **

10. Codificar el método de cruce (crossOperator).

11. Codificar el método de mutación (mutation).

12. Codificar el método de reemplazamiento (populationReplacement) -> Estacionario vs. Generacional.

13. En initializePopulation, incluir la plataforma del final del nivel.